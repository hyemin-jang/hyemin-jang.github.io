name : gatsby deploy

on:
  push:
    branches: 
      - gh-pages
  
jobs:
  deploy:
    name : deploy
    runs-on : ubuntu-latest  
    steps:  
      - name: checkout code
        uses: actions/checkout@v2

      - name: packages install
        run: yarn install

      - name: gatsby build
        run: yarn build
        env:
          GH_API_KEY : BLOG
        
      - name: deploy
        uses: maxheld93/ghpages@v0.2.1
        env:
          GITHUB_TOKEN : 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
          GH_PAT : BLOG
          BUILD_DIR : 'public/'
